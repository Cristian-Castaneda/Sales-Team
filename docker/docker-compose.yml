services:
  openclaw:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    user: "0:0"
    # Secrets are injected by Doppler at runtime via: doppler run -- docker compose up -d
    # Do NOT add env_file here — no .env file on disk.
    volumes:
      - /root/.openclaw:/root/.openclaw
      - /root/.openclaw:/home/node/.openclaw
    ports:
      - "127.0.0.1:18789:18789"
      - "127.0.0.1:18790:18790"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    depends_on:
      - browser

  browser:
    image: ghcr.io/browserless/chromium:latest
    restart: unless-stopped
    shm_size: "1gb"
    ports:
      - "127.0.0.1:3000:3000"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
